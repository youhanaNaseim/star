name: Run command

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to Echo'

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Message: $MESSAGE"
        env:
          MESSAGE: ${{ inputs.message }}